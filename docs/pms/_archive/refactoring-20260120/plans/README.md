# Phase 2: 리팩터링 계획서

> 최종 업데이트: 2026-01-20  
> 상태: 계획 수립 완료

---

## 🎯 Phase 2 목표

1. Phase 1 분석 결과를 바탕으로 **구체적 실행 계획** 수립
2. **개발 표준**에 따른 코드 품질 개선 계획 포함
3. **우선순위와 순서**를 명확히 하여 안전한 실행 보장
4. **검증 게이트**를 통한 각 단계 품질 확인

---

## 📋 실행 순서 및 우선순위

### 실행 원칙

```
의존성 역순: database → types → server → web
이유: 기초(하위)부터 수정해야 상위 레이어에 영향 최소화
```

### 우선순위 체계

| 등급 | 의미 | 기준 |
|------|------|------|
| **P0** | 즉시 | 기능 오류, 보안 취약점 |
| **P1** | 단기 | 타입 안전성, 일관성 |
| **P2** | 중기 | 코드 품질, 재사용성 |
| **P3** | 장기 | 유지보수성, 확장성 |

---

## 📊 전체 작업 목록

### 1. packages/database (P0)

| # | 작업 | 우선순위 | 영향 범위 | 계획서 |
|---|------|:--------:|----------|--------|
| DB-01 | Extension 사용 예제 추가 | P2 | 문서 | [database-plan.md](database-plan.md) |

> ✅ 이 패키지는 이미 우수한 상태 (9.8/10)

---

### 2. packages/types (P0)

| # | 작업 | 우선순위 | 영향 범위 | 계획서 |
|---|------|:--------:|----------|--------|
| TYPE-01 | Project 타입 정합화 | P0 | types, server, web | [types-plan.md](types-plan.md) |
| TYPE-02 | Index export 정리 | P3 | types | [types-plan.md](types-plan.md) |

---

### 3. apps/server (P0-P1)

| # | 작업 | 우선순위 | 영향 범위 | 계획서 |
|---|------|:--------:|----------|--------|
| SRV-01 | DatabaseService Extension 적용 | P0 | 전체 서버 | [server-plan.md](server-plan.md) |
| SRV-02 | ProjectController JwtAuthGuard | P0 | project API | [server-plan.md](server-plan.md) |
| SRV-03 | 응답 헬퍼 함수 공용화 | P1 | 전체 controller | [server-plan.md](server-plan.md) |
| SRV-04 | 응답 형식 통일 | P1 | 전체 controller | [server-plan.md](server-plan.md) |

---

### 4. apps/web-pms (P2-P3)

| # | 작업 | 우선순위 | 영향 범위 | 계획서 |
|---|------|:--------:|----------|--------|
| WEB-01 | 레거시 PageHeader 마이그레이션 | P2 | 사용 페이지 | [web-plan.md](web-plan.md) |
| WEB-02 | 레거시 ListPageTemplate 마이그레이션 | P2 | 사용 페이지 | [web-plan.md](web-plan.md) |
| WEB-03 | components/index.ts 정리 | P3 | import 경로 | [web-plan.md](web-plan.md) |
| WEB-04 | 로컬 타입 @ssoo/types로 이동 | P3 | types | [web-plan.md](web-plan.md) |

---

## 🔄 실행 흐름도

```
┌──────────────────────────────────────────────────────────────┐
│                    Phase 2: 계획 수립 완료                    │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│ Step 1: packages/types (P0)                                  │
│ ├── TYPE-01: Project 타입 정합화                            │
│ ├── TYPE-02: DoneResultCode 동기화                           │
│ └── TYPE-03: ProjectStatusCode 보완                          │
│                                                              │
│ 검증: pnpm -r exec tsc --noEmit                             │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│ Step 2: apps/server (P0)                                     │
│ ├── SRV-01: DatabaseService Extension 적용                   │
│ └── SRV-02: ProjectController JwtAuthGuard                   │
│                                                              │
│ 검증: pnpm run build:server                                  │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│ Step 3: apps/server (P1)                                     │
│ ├── SRV-03: 응답 헬퍼 함수 공용화                             │
│ └── SRV-04: 응답 형식 통일                                    │
│                                                              │
│ 검증: pnpm run build:server                                  │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│ Step 4: apps/web-pms (P2)                                        │
│ ├── WEB-01: 레거시 PageHeader 마이그레이션                    │
│ └── WEB-02: 레거시 ListPageTemplate 마이그레이션              │
│                                                              │
│ 검증: pnpm run build:web-pms                                     │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│ Step 5: 정리 작업 (P3)                                       │
│ ├── TYPE-04: Index export 정리                               │
│ ├── WEB-03: components/index.ts 정리                         │
│ └── WEB-04: 로컬 타입 이동                                    │
│                                                              │
│ 검증: pnpm run build                                         │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│                    Phase 3: 실행 완료                         │
│                    Phase 4: 최종 검증                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 🛡️ 안전장치

### 1. Git 브랜치 전략

```bash
# 리팩터링 전용 브랜치 생성
git checkout -b refactor/phase-3

# 각 Step별 커밋
git commit -m "refactor(types): align project types with schema"
git commit -m "refactor(server): apply database extension"
git commit -m "refactor(server): add JwtAuthGuard to ProjectController"
```

### 2. 검증 게이트

| 단계 | 명령어 | 통과 기준 |
|------|--------|----------|
| 타입 체크 | `pnpm -r exec tsc --noEmit` | 에러 0개 |
| 린트 | `pnpm run lint` | 에러 0개 |
| 빌드 | `pnpm run build` | 성공 |
| 테스트 | `pnpm run test` (있을 경우) | 통과 |

### 3. 롤백 계획

```bash
# 문제 발생 시 즉시 롤백
git checkout .
git clean -fd

# 또는 특정 커밋으로 복귀
git reset --hard <commit-hash>
```

---

## 📁 계획서 파일 구조

```
docs/_archive/refactoring-20260120/plans/
├── README.md                 # ← 현재 문서 (전체 계획 개요)
├── types-plan.md            # packages/types 세부 계획
├── server-plan.md           # apps/server 세부 계획
└── web-plan.md              # apps/web-pms 세부 계획
```

---

## ✅ 체크리스트

### Phase 2 완료 조건

- [x] 개발 표준 문서 작성 (development-standards.md)
- [x] 코드 품질 분석 완료 (analysis/code-quality.md)
- [x] 전체 계획 개요 작성 (plans/README.md)
- [ ] types 세부 계획 작성 (plans/types-plan.md)
- [ ] server 세부 계획 작성 (plans/server-plan.md)
- [ ] web 세부 계획 작성 (plans/web-plan.md)
- [ ] 사용자 승인

---

## 📌 다음 단계

Phase 2 계획 승인 후:

1. `refactor/phase-3` 브랜치 생성
2. Step 1부터 순차적 실행
3. 각 Step 완료 후 검증 게이트 통과 확인
4. EXECUTION_LOG.md에 실행 이력 기록
5. Phase 4에서 전체 검증 및 머지
