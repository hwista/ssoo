# 모듈러 모놀리스 아키텍처

> 최종 업데이트: 2026-02-03

---

## 개요

SSOO 백엔드는 **모듈러 모놀리스(Modular Monolith)** 아키텍처를 채택합니다.  
단일 배포 단위를 유지하면서, 내부는 도메인 단위로 명확히 분리하여 확장성과 유지보수성을 확보합니다.

### 왜 모듈러 모놀리스인가?

```
┌─────────────────────────────────────────────────────────────┐
│                     전통적 모놀리스                          │
│  - 빠른 개발, 단순한 배포                                    │
│  - 규모 커지면 의존성 엉킴, 변경 영향 파악 어려움              │
└─────────────────────────────────────────────────────────────┘
                           ↓ 단점 보완
┌─────────────────────────────────────────────────────────────┐
│                     모듈러 모놀리스                          │
│  - 도메인별 모듈 분리 (명확한 경계)                          │
│  - 단일 배포 유지 (운영 복잡도 ↓)                            │
│  - 마이크로서비스 전환 용이 (필요 시)                         │
└─────────────────────────────────────────────────────────────┘
                           ↓ 필요 시 전환
┌─────────────────────────────────────────────────────────────┐
│                     마이크로서비스                           │
│  - 독립 배포, 독립 확장                                      │
│  - 운영 복잡도 ↑, 네트워크 오버헤드                          │
└─────────────────────────────────────────────────────────────┘
```

**SSOO의 선택:**  
현재 규모에서 마이크로서비스는 오버엔지니어링. 모듈러 모놀리스로 경계를 명확히 유지하다가, 트래픽/팀 규모가 커지면 특정 모듈만 분리 가능.

---

## 모듈 구조

### 디렉토리 레이아웃

```
apps/server/src/
├── main.ts                    # 진입점
├── app.module.ts              # 루트 모듈
│
├── common/                    # 🔧 공용 인프라 레이어
│   ├── filters/               #   - 전역 예외 필터
│   ├── interceptors/          #   - 요청 컨텍스트 인터셉터
│   ├── swagger/               #   - Swagger 데코레이터
│   ├── utils/                 #   - BigInt 직렬화 등
│   └── responses.ts           #   - ApiSuccess/ApiError 응답 래퍼
│
├── config/                    # ⚙️ 환경 설정
│   └── config.validation.ts   #   - Joi 기반 환경변수 검증
│
├── database/                  # 🗄️ 데이터 접근 레이어
│   ├── database.module.ts     #   - Prisma 모듈
│   └── prisma.service.ts      #   - Prisma 클라이언트 서비스
│
└── modules/                   # 📦 도메인 모듈
    ├── common/                #   - 공통 도메인 (인증, 사용자)
    ├── pms/                   #   - PMS 도메인 (메뉴, 프로젝트)
    └── dms/                   #   - DMS 도메인 (문서 관리) [슬롯]
```

### 도메인 모듈 상세

#### common 모듈 (공통 도메인)

모든 시스템에서 공유하는 **인증/사용자** 관련 기능.

```
modules/common/
├── common.module.ts           # 모듈 정의
├── auth/                      # 인증
│   ├── auth.module.ts
│   ├── auth.controller.ts     # POST /api/auth/login, /refresh, /logout
│   ├── auth.service.ts
│   ├── strategies/            # JWT, Local 전략
│   ├── guards/                # JwtAuthGuard
│   ├── decorators/            # @CurrentUser
│   └── dto/
├── user/                      # 사용자
│   ├── user.module.ts
│   ├── user.controller.ts     # GET/POST /api/users
│   ├── user.service.ts
│   └── dto/
└── health/                    # 헬스체크
    └── health.controller.ts   # GET /api/health
```

**DB 스키마:** `common` (User, UserHistory)

#### pms 모듈 (프로젝트 관리 도메인)

PMS 전용 비즈니스 로직.

```
modules/pms/
├── pms.module.ts              # 모듈 정의
├── menu/                      # 메뉴/권한
│   ├── menu.module.ts
│   ├── menu.controller.ts     # GET /api/menus/my, /tree
│   ├── menu.service.ts
│   └── dto/
└── project/                   # 프로젝트
    ├── project.module.ts
    ├── project.controller.ts  # CRUD /api/projects
    ├── project.service.ts
    └── dto/
```

**DB 스키마:** `pms` (Menu, RoleMenu, Project, Customer, 등)

#### dms 모듈 (문서 관리 도메인) [미구현]

DMS 전용 비즈니스 로직. 현재는 슬롯만 준비.

```
modules/dms/                   # 비어있음 - 향후 구현
```

**DB 스키마:** `dms` (향후 추가)

---

## 모듈 의존성 규칙

### 핵심 원칙

```
                    ┌─────────────┐
                    │ app.module  │
                    └──────┬──────┘
                           │ imports
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ common   │ │   pms    │ │   dms    │
        │ Module   │ │  Module  │ │  Module  │
        └──────────┘ └──────────┘ └──────────┘
              │            │            │
              │            ▼            │
              │      ┌──────────┐       │
              └─────▶│ database │◀──────┘
                     │  Module  │
                     └──────────┘
```

### 의존성 방향

| From | To | 허용 여부 |
|------|-----|----------|
| `pms` | `common` | ❌ 금지 |
| `common` | `pms` | ❌ 금지 |
| `pms` | `dms` | ❌ 금지 |
| `*` | `database` | ✅ 허용 |
| `*` | `common/` (인프라) | ✅ 허용 |

> **원칙:** 도메인 모듈 간 직접 의존 금지. 공유 필요 시 `database` 또는 이벤트 기반 통신 사용.

### ESLint 강제

[eslint.config.mjs](../../../apps/server/eslint.config.mjs)에서 모듈 경계를 린트 규칙으로 강제합니다:

```javascript
'import/no-restricted-paths': [
  'error',
  {
    zones: [
      {
        target: './src/modules/common',
        from: './src/modules/pms',
        message: 'pms 모듈은 common을 참조할 수 있지만 common은 pms를 참조할 수 없습니다.',
      },
      {
        target: './src/modules/pms',
        from: './src/modules/common',
        message: '도메인 의존성은 한 방향(pms -> common)만 허용됩니다.',
      },
    ],
  },
],
```

**위반 시 빌드/린트 실패.**

---

## 모듈 간 통신

### 현재 방식: 직접 호출 없음

도메인 모듈끼리 직접 import하지 않습니다.  
데이터 공유가 필요하면 `database` 레이어를 통해 접근합니다.

```typescript
// ✅ 올바른 방식: database 레이어 사용
@Injectable()
export class ProjectService {
  constructor(private prisma: PrismaService) {}

  async createProject(dto: CreateProjectDto, userId: bigint) {
    // User는 common 스키마에 있지만, prisma를 통해 접근
    return this.prisma.project.create({
      data: {
        ...dto,
        ownerId: userId,  // userId는 컨트롤러에서 @CurrentUser로 받음
      },
    });
  }
}
```

```typescript
// ❌ 잘못된 방식: 도메인 모듈 직접 import
import { UserService } from '../../common/user/user.service';  // ESLint 에러!
```

### 향후 확장: 이벤트 기반 통신

규모가 커지면 NestJS EventEmitter 또는 메시지 큐를 도입할 수 있습니다:

```typescript
// 예시: 프로젝트 생성 시 이벤트 발행
this.eventEmitter.emit('project.created', { projectId, ownerId });

// common 모듈에서 이벤트 구독 (필요 시)
@OnEvent('project.created')
handleProjectCreated(payload: ProjectCreatedEvent) {
  // 사용자 활동 로그 기록 등
}
```

---

## DB 스키마 매핑

모듈과 PostgreSQL 스키마는 1:1 대응합니다:

| 모듈 | DB 스키마 | 테이블 접두사 | 테이블 수 |
|------|-----------|--------------|-----------|
| `common` | `common` | `cm_user_*` | 2개 |
| `pms` | `pms` | `cm_*`, `pr_*` | 27개 |
| `dms` | `dms` | `dm_*` | 0개 (예정) |

자세한 내용은 [database README](../../../packages/database/README.md) 참조.

---

## 새 모듈 추가 가이드

### 1. 모듈 디렉토리 생성

```bash
mkdir -p apps/server/src/modules/newdomain
```

### 2. 모듈 파일 구조

```
modules/newdomain/
├── newdomain.module.ts
├── feature1/
│   ├── feature1.module.ts
│   ├── feature1.controller.ts
│   ├── feature1.service.ts
│   └── dto/
└── feature2/
    └── ...
```

### 3. 모듈 정의

```typescript
// newdomain.module.ts
import { Module } from '@nestjs/common';
import { Feature1Module } from './feature1/feature1.module';

@Module({
  imports: [Feature1Module],
  exports: [Feature1Module],
})
export class NewDomainModule {}
```

### 4. 루트 모듈에 등록

```typescript
// app.module.ts
import { NewDomainModule } from './modules/newdomain/newdomain.module';

@Module({
  imports: [
    // ...
    CommonModule,
    PmsModule,
    NewDomainModule,  // 추가
  ],
})
export class AppModule {}
```

### 5. ESLint 규칙 추가 (필요 시)

새 모듈과 기존 모듈 간 의존성 규칙을 추가합니다.

### 6. DB 스키마 추가 (필요 시)

```prisma
// schema.prisma
datasource db {
  schemas = ["common", "pms", "dms", "newdomain"]  // 추가
}

model NewEntity {
  // ...
  @@schema("newdomain")
}
```

---

## 관련 문서

- [tech-stack.md](./tech-stack.md) - 기술 스택 개요
- [development-standards.md](./development-standards.md) - 개발 표준
- [database README](../../../packages/database/README.md) - DB 스키마 상세
- [auth-system.md](./auth-system.md) - 인증 시스템

---

## Changelog

| 날짜 | 변경 내용 |
|------|----------|
| 2026-01-23 | 모듈러 모놀리스 아키텍처 문서 최초 작성 |
