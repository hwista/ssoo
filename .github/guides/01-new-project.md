# 01. 신규 프로젝트 시작 가이드

> 빈 폴더에서 SDD Framework로 프로젝트를 시작하는 방법

---

## 개요

이 가이드는 **빈 폴더 + .github 파일만으로** 완전한 프로젝트를 구축하는 과정을 설명합니다.

### 핵심 원칙

1. **점검 우선** - 모든 단계에서 현황 파악이 먼저
2. **검증 루프** - 100% 달성까지 반복
3. **정본 = .github** - 이 문서들로 모든 것을 빌드할 수 있어야 함

---

## 전체 흐름

```
┌─────────────────────────────────────────────────────────────┐
│  Phase 0: 준비                                               │
│  → .github 폴더 복사, 환경 설정                              │
├─────────────────────────────────────────────────────────────┤
│  Phase 1: 요구사항 분석                                      │
│  → 서비스 정의, 기술 스택 선정, 아키텍처 결정                │
├─────────────────────────────────────────────────────────────┤
│  Phase 2: 프로젝트 초기화                                    │
│  → 폴더 구조, 설정 파일, copilot-instructions.md            │
├─────────────────────────────────────────────────────────────┤
│  Phase 3: 개발 시작                                          │
│  → SDD 워크플로우에 따른 반복 개발                           │
└─────────────────────────────────────────────────────────────┘
```

---

## Phase 0: 준비

### 0.1 SDD Framework 코어 복사

> ⚠️ **중요**: 전체 `.github/`를 복사하면 레포 특화 파일도 포함됩니다.
> **코어만 선택적으로 복사**하세요.

```bash
# 빈 폴더 준비
mkdir my-project && cd my-project
git init

# 코어만 선택적 복사
mkdir -p .github/prompts
cp -r [SDD_SOURCE]/.github/scripts .github/
cp -r [SDD_SOURCE]/.github/agents .github/
cp -r [SDD_SOURCE]/.github/prompts/core .github/prompts/
cp -r [SDD_SOURCE]/.github/guides .github/
cp -r [SDD_SOURCE]/.github/templates .github/
cp [SDD_SOURCE]/.github/README.md .github/

# 첫 커밋
git add .github
git commit -m "chore: SDD Framework 코어 초기화"
```

### 0.2 코어 vs 레포 특화 파일

| 구분 | 경로 | 복사 방법 |
|------|------|----------|
| ✅ **코어** | `scripts/`, `agents/`, `prompts/core/`, `guides/`, `templates/` | 그대로 복사 |
| 🔶 **레포 특화** | `copilot-instructions.md`, `instructions/`, `prompts/[project]/` | 템플릿에서 생성 |

### 0.3 필수 파일 확인

```
.github/
├── README.md                    # ✅ 필수
├── scripts/                     # ✅ 필수 (검증 스크립트)
│   └── sdd-verify.js
├── agents/                      # ✅ 필수 (범용)
│   ├── common-workflow.md
│   ├── orchestrator.agent.md
│   └── ... (모든 에이전트)
├── prompts/
│   └── core/                    # ✅ 필수 (범용)
│       ├── project-init.prompt.md
│       ├── feature-dev.prompt.md
│       └── ...
├── guides/                      # ✅ 필수
│   ├── 01-new-project.md       # 현재 문서
│   └── 03-tech-stack.md
└── templates/                   # ✅ 필수
    ├── copilot-instructions/
    ├── folder-structure/
    ├── instructions/            # 🆕
    ├── config/                  # 🆕
    ├── docs/                    # 🆕
    └── workflows/               # 🆕
```

### 0.4 환경 확인

```bash
# AI 환경
- GitHub Copilot Chat 활성화 확인
- Agent 호출 테스트: @orchestrator ping
```

---

## Phase 1: 요구사항 분석

### 1.1 서비스 정의

AI와 대화하며 서비스를 정의합니다:

```markdown
@planner

## 서비스 정의

### 프로젝트명
[프로젝트명]

### 목적
[한 문장으로 서비스 목적]

### 핵심 기능 (MVP)
1. [기능 1]
2. [기능 2]
3. [기능 3]

### 사용자 유형
- [사용자 1]: [역할/권한]
- [사용자 2]: [역할/권한]

### 제약 조건
- 예산: [예산]
- 일정: [마감일]
- 기술 제약: [있다면]
```

### 1.2 기술 스택 선정

👉 **상세 가이드**: [03-tech-stack.md](03-tech-stack.md)

```markdown
@architect

## 기술 스택 선정 요청

### 서비스 특성
- 유형: [웹앱/모바일/API/...]
- 규모: [소/중/대]
- 동시 사용자: [예상치]
- 데이터 특성: [RDBMS/NoSQL/...]

### 팀 역량
- 익숙한 언어: [TypeScript/Python/.NET/...]
- 경험 프레임워크: [React/Vue/...]

### 의사결정 요청
1. 프론트엔드 프레임워크
2. 백엔드 프레임워크
3. 데이터베이스
4. 배포 환경
```

**AI 응답 예시:**

```markdown
## 권장 스택

| 레이어 | 선택 | 이유 |
|--------|------|------|
| 프론트엔드 | Next.js 15 (App Router) | SSR/SSG, React 생태계 |
| 백엔드 | NestJS 10 | 모듈러 구조, TypeScript |
| DB | PostgreSQL + Prisma | 타입 안정성, 마이그레이션 |
| 배포 | Vercel + Railway | 간편한 설정, 비용 효율 |

### 대안 비교표
[A vs B vs C 비교...]

### 사용자 확인 필요
- [ ] 위 스택으로 진행하시겠습니까?
```

### 1.3 아키텍처 결정

```markdown
@architect

## 아키텍처 결정

### 선정된 스택
[위에서 결정된 스택]

### 결정 필요 사항
1. 모놀리스 vs 마이크로서비스
2. 모노레포 vs 멀티레포
3. API 스타일 (REST/GraphQL)

### 제약 조건
- 팀 규모: [N명]
- 초기 개발 속도 중요도: [높음/중간]
```

---

## Phase 2: 프로젝트 초기화

### 2.0 템플릿에서 기본 파일 생성

```bash
# copilot-instructions.md 생성
cp .github/templates/copilot-instructions/_base.md .github/copilot-instructions.md

# 기술 스택에 맞는 config 선택 후 복사
# TypeScript 모노레포 (pnpm + Turborepo)
cp -r .github/templates/config/typescript-monorepo/* ./

# 또는 다른 스택:
# cp -r .github/templates/config/typescript-npm/* ./
# cp -r .github/templates/config/python-poetry/* ./
# cp -r .github/templates/config/dotnet/* ./

# docs 초기 구조 복사
cp -r .github/templates/docs ./docs/

# CI 워크플로우 복사
mkdir -p .github/workflows
cp .github/templates/workflows/pr-validation.yml .github/workflows/

# instructions 템플릿에서 경로별 규칙 생성
mkdir -p .github/instructions
cp .github/templates/instructions/_base.server.md .github/instructions/server.instructions.md
cp .github/templates/instructions/_base.web.md .github/instructions/web.instructions.md
cp .github/templates/instructions/_base.database.md .github/instructions/database.instructions.md

# 커밋
git add .
git commit -m "chore: 프로젝트 기본 구조 생성"
```

### 2.1 프로젝트 초기화 프롬프트

👉 **프롬프트 템플릿**: [project-init.prompt.md](../prompts/core/project-init.prompt.md)

```markdown
@orchestrator

#file:prompts/core/project-init.prompt.md

## 프로젝트 초기화 요청

### 확정된 스택
- 프론트엔드: [Next.js 15]
- 백엔드: [NestJS 10]
- 데이터베이스: [PostgreSQL + Prisma]
- 구조: [모노레포 + 모듈러 모놀리스]

### 프로젝트명
[my-project]

### 앱 구성
- web-app: 웹 클라이언트 (Next.js)
- server: 백엔드 API 서버 (NestJS)

### 생성 요청
1. 폴더 구조
2. 패키지 설정 (package.json)
3. TypeScript 설정
4. ESLint/Prettier 설정
5. copilot-instructions.md
6. instructions/*.md
```

### 2.2 생성되는 파일들

```
my-project/
├── .github/
│   ├── copilot-instructions.md   # ← 프로젝트 특화 규칙
│   ├── instructions/             # ← 경로별 규칙
│   │   ├── server.instructions.md
│   │   ├── web.instructions.md
│   │   └── ...
│   ├── prompts/
│   │   ├── core/                 # ← 범용 (복사됨)
│   │   └── [project]/            # ← 프로젝트 특화
│   └── ...
├── apps/
│   ├── server/
│   └── web/
├── packages/
│   ├── database/
│   └── types/
├── docs/                         # ← AI가 생성할 문서 위치
├── package.json
├── pnpm-workspace.yaml
└── turbo.json
```

### 2.3 검증 체크리스트

```markdown
## Phase 2 검증

### 구조 검증
- [ ] 폴더 구조가 템플릿과 일치하는가?
- [ ] 모든 패키지에 package.json이 있는가?
- [ ] TypeScript 설정이 올바른가?

### 규칙 검증
- [ ] copilot-instructions.md가 프로젝트에 맞게 커스터마이징되었는가?
- [ ] instructions 파일들이 경로별로 존재하는가?

### 동작 검증
- [ ] `pnpm install` 성공?
- [ ] `pnpm build` 성공?
- [ ] `pnpm lint` 오류 없음?

### 미달 시
→ 오류 내용을 AI에게 전달하여 수정
→ 100% 달성까지 반복
```

---

## Phase 3: 개발 시작

### 3.1 SDD 워크플로우 시작

이제 표준 SDD 워크플로우를 따릅니다:

```
스펙 → 검증 → 계획 → 검증 → 실행 → 검증 → 분석 → 핸드오프
```

### 3.2 첫 번째 기능 개발

```markdown
@orchestrator

#file:prompts/core/feature-dev.prompt.md

## 첫 번째 기능: 사용자 인증

### 요구사항
1. 이메일/비밀번호 로그인
2. JWT 토큰 발급
3. 토큰 갱신
4. 로그아웃

### 참조
- 없음 (첫 기능)

### 완료 조건
1. ✅ API 구현
2. ✅ 프론트엔드 로그인 페이지
3. ✅ 테스트 케이스
4. ✅ 문서화
```

### 3.3 개발 사이클

```
┌─────────────────────────────────────────────────────────────┐
│  1. 기능 개발    →  @orchestrator → 에이전트 체인           │
├─────────────────────────────────────────────────────────────┤
│  2. 코드 검증    →  @reviewer → 패턴 일치, Dead Code 확인   │
├─────────────────────────────────────────────────────────────┤
│  3. 품질 측정    →  현재 수준 측정 (린트, 빌드, 테스트)      │
├─────────────────────────────────────────────────────────────┤
│  4. 100% 달성?   →  NO: 3.2로 돌아가 반복                   │
│                  →  YES: 다음 기능으로                       │
├─────────────────────────────────────────────────────────────┤
│  5. 문서화       →  docs/ 업데이트, 커밋                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 품질 수렴 루프

### 측정 지표

| 지표 | 측정 방법 | 목표 |
|------|----------|------|
| 린트 오류 | `pnpm lint` | 0개 |
| 타입 오류 | `pnpm typecheck` | 0개 |
| 빌드 성공 | `pnpm build` | 100% |
| 테스트 통과 | `pnpm test` | 100% |
| 패턴 일치 | `check-patterns.js` | 100% |

### 수렴 프로세스

```
while (quality < 100%) {
  1. 현재 수준 측정
  2. 미달 항목 식별
  3. AI에게 수정 요청
  4. 수정 결과 검증
  5. 반복
}
```

### 미달 시 프롬프트 예시

```markdown
@developer

## 품질 미달 수정 요청

### 현재 상태
- 린트 오류: 3개
- 패턴 불일치: hooks/index.ts (와일드카드 export)

### 오류 상세
[오류 메시지 붙여넣기]

### 수정 요청
위 오류를 모두 해결해주세요.

### 검증 방법
수정 후 `pnpm lint && pnpm build` 실행
```

---

## 문서 정본 원칙

### .github (정본)
- 모든 개발 규칙의 원천
- 이것만으로 프로젝트 빌드 가능해야 함
- 범용 원칙 + 프로젝트별 확장

### docs/ (결과물)
- AI가 생성하는 서비스 설명 문서
- 독립적으로 읽어도 이해 가능
- 서비스/도메인 컨텍스트 포함

```
.github/                          docs/
┌─────────────────────┐          ┌─────────────────────┐
│ "이걸로 빌드한다"   │  ←상호→  │ "이걸로 이해한다"   │
│                     │   보완   │                     │
│ - 개발 규칙        │          │ - 서비스 설명       │
│ - 프롬프트         │          │ - API 문서          │
│ - 검증 기준        │          │ - 도메인 개념       │
└─────────────────────┘          └─────────────────────┘
```

---

## Changelog

| 날짜 | 변경 내용 |
|------|----------|
| 2026-02-05 | 초기 버전 - 신규 프로젝트 시작 가이드 |

