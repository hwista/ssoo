#!/usr/bin/env sh
set -e

if command -v node.exe >/dev/null 2>&1; then
  exec node.exe "$@"
fi

if [ -x "/mnt/c/Program Files/nodejs/node.exe" ]; then
  exec "/mnt/c/Program Files/nodejs/node.exe" "$@"
fi

echo "husky: node executable not found. Install Node.js or update PATH." >&2
exit 1
