/**
 * Assistant entry barrel
 *
 * `common/assistant` 는 DMS 전역/페이지 공통 assistant 기능 모듈 모음입니다.
 * 현재는 UI 조각과 세션/chat orchestration 이 함께 존재하므로, broad barrel export 는 의도적으로 제한합니다.
 */
export { FloatingAssistant } from './FloatingAssistant';
