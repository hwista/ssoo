'use client';

import { SsooPanelFrame } from '@ssoo/web-shell';
import type { SsooPanelFrameProps } from '@ssoo/web-shell';

export type PanelFrameProps = SsooPanelFrameProps;

export const PanelFrame = SsooPanelFrame;
